"use client";
import {FormEvent,Suspense,useState} from "react";import {useSearchParams} from "next/navigation";import Header from "@/components/Header";import Footer from "@/components/Footer";import {confirmPasswordReset} from "@/lib/api";
function ResetForm(){const token=useSearchParams().get("token")??"";const [message,setMessage]=useState<string|null>(null);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const d=new FormData(event.currentTarget);if(d.get("password")!==d.get("confirm")){setMessage("Passwords do not match.");return;}await confirmPasswordReset(token,String(d.get("password")));setMessage("Password changed. You can now sign in.");}return <form onSubmit={submit} className="mu-panel mx-auto my-20 max-w-md rounded-xl p-8"><div className="relative space-y-5"><h1 className="text-3xl font-black">Choose a new password</h1><input name="password" type="password" minLength={10} required placeholder="New password" className="w-full rounded border border-white/10 bg-black/40 px-4 py-3"/><input name="confirm" type="password" minLength={10} required placeholder="Confirm password" className="w-full rounded border border-white/10 bg-black/40 px-4 py-3"/>{message&&<p>{message}</p>}<button className="w-full rounded bg-amber-400 py-3 font-black text-black">CHANGE PASSWORD</button></div></form>;}export default function ResetPage(){return <main className="min-h-screen text-white"><Header/><Suspense fallback={<div className="mx-auto my-20 max-w-md text-center">Loading…</div>}><ResetForm/></Suspense><Footer/></main>;}
